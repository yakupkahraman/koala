import 'package:flutter/material.dart';

class BHomePage extends StatelessWidget {
  const BHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Business Home Page')));
  }
}
