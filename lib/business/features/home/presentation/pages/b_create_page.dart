import 'package:flutter/material.dart';

class BCreatePage extends StatelessWidget {
  const BCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar());
  }
}
